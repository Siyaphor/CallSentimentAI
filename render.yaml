services:
  - type: web
    name: voiceiq
    runtime: docker
    plan: starter
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DATABASE
        value: voiceiq
      - key: MONGODB_COLLECTION
        value: call_history
